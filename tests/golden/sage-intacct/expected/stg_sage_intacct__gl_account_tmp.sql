-- ** Values passed to adapter.get_relation:
    -- full-identifier_var: sage_intacct_gl_account_identifier
    -- database: sage_intacct                            
    -- schema: sage_intacct_integration_tests
    -- identifier: sage_intacct__gl_account


    
        

        select
            cast(null as TEXT) as _dbt_source_relation
        limit 0
